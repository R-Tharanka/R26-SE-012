# Training-only dependencies for Phase 3 real model training.
#
# Keep TensorFlow out of backend runtime. Use this file in a dedicated training venv.
#
# Tested target environment: Python 3.13 (CPU) with TensorFlow 2.21+.
# TensorFlow 2.21 provides cp313 wheels on Windows (Python 3.13).
tensorflow==2.21.0

# tf2onnx 1.16.x pins protobuf~=3.20 (no py313 wheels) and conflicts with TF 2.21.
# Use a newer tf2onnx compatible with modern protobuf.
tf2onnx==1.17.0

# Utilities
numpy>=2.0
Pillow>=10.0
matplotlib>=3.8
scikit-learn>=1.8.0
psutil>=5.9
